#include <bits/stdc++.h>
using namespace std;

double a, b;

int main(void) {
    cin >> a >> b;
    cout << fixed << setprecision(9); // cout.precision(9) <- 정수+소수 총 9자리
    cout << a / b;
}
