#include <bits/stdc++.h>

using namespace std;

int n, sum;

int main(void) {
    cin >> n;
    cout << n * (n + 1) / 2;
    /*
    for(int i = 0; i < n + 1; i++)
        sum += i;
    cout << sum << '\n';
    */
}